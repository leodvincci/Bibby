import BitArray from '@zxing/library/es2015/core/common/BitArray';
import Token from '@zxing/library/es2015/core/aztec/encoder/Token';
import { int } from '@zxing/library/es2015/customTypings';
export default class SimpleToken extends Token {
    private value;
    private bitCount;
    constructor(previous: Token, value: int, bitCount: int);
    /**
     * @Override
     */
    appendTo(bitArray: BitArray, text: Uint8Array): void;
    add(value: int, bitCount: int): Token;
    addBinaryShift(start: int, byteCount: int): Token;
    /**
     * @Override
     */
    toString(): String;
}
