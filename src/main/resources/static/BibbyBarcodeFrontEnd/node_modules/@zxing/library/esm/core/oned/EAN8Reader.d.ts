import BarcodeFormat from '@zxing/library/esm/core/BarcodeFormat';
import BitArray from '@zxing/library/esm/core/common/BitArray';
import UPCEANReader from '@zxing/library/esm/core/oned/UPCEANReader';
/**
 * <p>Implements decoding of the EAN-8 format.</p>
 *
 * @author Sean Owen
 */
export default class EAN8Reader extends UPCEANReader {
    private decodeMiddleCounters;
    constructor();
    decodeMiddle(row: BitArray, startRange: Int32Array, resultString: string): {
        rowOffset: number;
        resultString: string;
    };
    getBarcodeFormat(): BarcodeFormat;
}
