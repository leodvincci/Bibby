import DecodedObject from '@zxing/library/es2015/core/oned/rss/expanded/decoders/DecodedObject';
export default class DecodedChar extends DecodedObject {
    private readonly value;
    static readonly FNC1 = "$";
    constructor(newPosition: number, value: string);
    getValue(): string;
    isFNC1(): boolean;
}
