import { BrowserCodeReader } from '@zxing/library/cjs/browser/BrowserCodeReader';
import MultiFormatReader from '@zxing/library/cjs/core/MultiFormatReader';
import BinaryBitmap from '@zxing/library/cjs/core/BinaryBitmap';
import Result from '@zxing/library/cjs/core/Result';
import DecodeHintType from '@zxing/library/cjs/core/DecodeHintType';
export declare class BrowserMultiFormatReader extends BrowserCodeReader {
    protected readonly reader: MultiFormatReader;
    constructor(hints?: Map<DecodeHintType, any>, timeBetweenScansMillis?: number);
    /**
     * Overwrite decodeBitmap to call decodeWithState, which will pay
     * attention to the hints set in the constructor function
     */
    decodeBitmap(binaryBitmap: BinaryBitmap): Result;
}
