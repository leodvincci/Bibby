import { TextDecoder } from '@zxing/text-encoding/es2015/common/TextDecoder';
import { TextEncoder } from '@zxing/text-encoding/es2015/common/TextEncoder';
// Polyfills browser
if (typeof window !== 'undefined') {
    const checkUndefined = key => !(key in window)
        || typeof window[key] === 'undefined'
        || window[key] === null;
    if (checkUndefined('TextDecoder'))
        window['TextDecoder'] = TextDecoder;
    if (checkUndefined('TextEncoder'))
        window['TextEncoder'] = TextEncoder;
}
export { TextDecoder, TextEncoder };
//# sourceMappingURL=encoding.js.map